<!-- name: missing_point_review; version: v1 -->

The crop already shows accepted points. Inspect only the owner core for qualifying instances not covered by those points. Do not duplicate existing points. Return JSON only; an empty `missing_points` list is valid.

裁剪图已标出接受点。只检查 owner core 内尚未被这些点覆盖、且符合目标规格的实例。不得重复已有点。只返回 JSON；`missing_points` 为空是合法结果。

```json
{"missing_points":[],"review_complete":true,"uncertainty":[]}
```
