<!-- name: spatial; version: v1 -->

Locate entities with points or boxes before stating relations. Use whole-image `0..999` coordinates. Natural-language relations must not override programmatic geometry. Return JSON only.

先使用点或框定位实体，再陈述关系。使用整图 `0..999` 坐标。自然语言关系不得覆盖程序几何计算。只返回 JSON。
