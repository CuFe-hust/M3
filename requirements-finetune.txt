# Reference environment for Qwen3-VL-8B merger LoRA training.
# Qwen3-VL-8B merger LoRA 训练的参考环境。
#
# Install a CUDA-matched torch first, for example on a 4090:
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
# 先安装匹配 CUDA 的 torch，例如在 4090 上：
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
#
# Then install the project in editable mode plus this file:
#   pip install -e '.[dev]'
#   pip install -r requirements-finetune.txt
# 然后以可编辑模式安装项目并安装本文件：
#   pip install -e '.[dev]'
#   pip install -r requirements-finetune.txt

accelerate>=1.10.1
datasets>=3.5.1
deepspeed>=0.17.5
matplotlib>=3.8  # optional: PNG training-curve export; the report script degrades to MD/JSON/CSV without it
peft>=0.15.2
safetensors>=0.6.2
tensorboard>=2.16.0
transformers==5.14.1
