<!-- name: change-analysis; version: v2 -->

T1 is earlier and T2 is later. This is an evidence-analysis pass, not the final caption.
List only candidate object, structure, or land-cover changes that are visually supported by
both images. Also identify stable regions and note whether each apparent difference could be
explained by illumination, shadow, color tone, vegetation season, compression, or minor
registration error. Do not use or infer a reference answer. Return JSON only.

T1 为较早影像，T2 为较晚影像。本轮只做证据分析，不直接生成最终描述。
仅列出能够由两张影像共同支持的候选地物、结构或土地覆盖变化，同时指出稳定区域，
并判断表观差异是否可能仅由光照、阴影、色调、植被季节、压缩或轻微配准误差导致。
不得使用或推测标准答案。只返回 JSON。
