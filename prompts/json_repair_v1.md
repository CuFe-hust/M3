<!-- name: json_repair; version: v1; schema: caller-provided -->

Return only corrected JSON. Preserve the original task and correct only the listed machine validation errors. Do not add claims, points, or facts that lack evidence in the prior response.

只输出修正后的 JSON。保留原任务，仅修复机器校验列出的错误；不得新增上次响应中没有证据支持的事实、点或结论。
