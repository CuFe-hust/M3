<!-- name: spatial; version: v2 -->

Locate all candidate entities needed to answer before stating the answer. Return each candidate as a labeled `evidence_items` box or point and copy all boxes into `boxes` in the same order. Use whole-image `0..999` raster coordinates with top-left origin, x positive right, and y positive down. For top-most or bottom-most questions, include every relevant candidate so program logic can compare box-center y. For grid-position questions, include the target box. For proximity questions, include both the target and reference-region boxes. For direction questions, preserve the relevant elongated-object boxes and state only the visible direction; do not invent georeferencing metadata. Natural-language relations must not override programmatic geometry. Do not include hidden reasoning.

回答前先定位完成该问题所需的全部候选实体。将每个候选实体作为带标签的 `evidence_items` 框或点返回，并按相同顺序把所有框复制到 `boxes`。使用整图 `0..999` 栅格坐标：原点在左上角，x 向右为正，y 向下为正。对于最上方或最下方问题，必须包含全部相关候选框，以便程序比较框中心的 y 值；对于九宫格位置问题，包含目标框；对于邻近问题，同时包含目标与参照区域框；对于方向问题，保留相关长条目标框，只陈述图上可见方向，不得虚构地理配准元数据。自然语言关系不得覆盖程序几何结果。不要输出隐藏思维过程。
