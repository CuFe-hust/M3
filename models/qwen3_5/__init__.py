"""Qwen3.5 model folder.
Qwen3.5 模型目录。

The wrapper reuses the shared Transformers client in
``models.qwen_transformers``; this folder provides a stable per-model import
path and keeps the Qwen3.5 weights under ``weights/``.
封装复用 ``models.qwen_transformers`` 中的共享 Transformers 客户端；
本目录提供稳定的按模型导入路径，并将 Qwen3.5 权重放在 ``weights/`` 下。
"""

__all__: list[str] = []
